上传流程 
分钟上传数据：
获取时间间隔
提取污水或者废气的最大，最小，平均值，累计值。
for（循环20次）
{
1判断数组里是否存在监测因子
 a：不存在contune进入下次循环（长度为5判断）。
 b：存在则运行以下内容：
	1，判断是否污水废气的因子。
		是：把结构和最大最小平均值传给字符串拼凑函数。
    	2，根据时间间隔数据库中读取该监测因子的最大最小等各项值。

	3，取出值后现判断污水或废气是否存在，
		a,存在计算累计值，不存在累计值设置成0；
		
	4，拼凑该因子各项值的字符串
		将结构类型及值传递给字符拼凑函数；
}

小时上传输据

字符串拼凑函数
根据传进来的值
根据传进来的值，类型（分钟，小时或日），结构拼凑字符串。返回凑完的字符串
